version: 2

sources:
  - name: evaluator
    schema: raw_admin
    description: product evaluator raw tables
    tables:
      - name: evaluator_rules_pipe
        description: Metadata for the evaluator rules