version: 2

sources:
  - name: project_evaluator
    description: project evaluator raw tables
    tables:
      - name: project_evaluator_metadata
        description: Metadata for the project evaluator to enable reporting
